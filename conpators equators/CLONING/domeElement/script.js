const tegEl = document.getElementsByTagName('h1');
console.log(tegEl)
const tagEl = document.getElementsByTagName('texting');
console.log(tagEl);

const checkId = document.getElementById('boxText');

const allEl = document.querySelectorAll('selectEl');

const oneEl = document.querySelector('selectElOne');

const ell = document.querySelector('peleZainab');

const submitBtn = document.querySelector('button');
console.log(submitBtn)

submitBtn.addEventListener('moseover',()=>{
    alert('Its closing time already')
})
